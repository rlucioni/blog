---
layout: base.liquid
---

{% for post in collections.post reversed %}
  <section>
    <h1>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </h1>
    <span class="date">
      {{ post.date | date: "%B %d, %Y" }}
    </span>
  </section>
{% endfor %}
