dist: xenial

language: python
python: 3.7

addons:
  snaps:
  - hugo

addons:
  snaps:
    - name: hugo
      channel: extended/stable

cache:
  pip: true
  directories:
    - $HOME/.npm

env:
  - NPM_VERSION=6.4.1
  - NVM_VERSION=0.34.0

install: true

script: ./scripts/build.sh

deploy:
  on:
    branch: master
  provider: script
  script: ./scripts/deploy.sh
  skip_cleanup: true
